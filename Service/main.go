tpackage main

import (
//	"context"
//	"module/Service/checker"
//      "fmt"
//	"time"
//	"module/Service/intermal/models"
)
/*
var str string
var l int

func GetMertics(){

}

type GoMetrClient struct {
        url string
        timeOut time.Duration
}

func (GMC *GoMetrClient) GetMetrics() {

}
*/
func main() {
//	f := "Hi!"
//	models.DataBaseLoad(f)

/*	items := &checker.Checker{}

	ctx, cancel := context.WithCancel(context.Background())
	change := make(chan int)
	//Присваивание ссылки(указателя) на структуру в переменную.
	var Checkable1 checker.Checkable
	//Объявление переменной как тип интерфейс.
	var Checkable2 checker.Checkable
	var Checkable3 checker.Checkable
	var Checkable4 checker.Checkable

	Checkable1 = &checker.HealthCheck{}
	//Добавление структуры в переменную-интерфейс.
	Checkable2 = &checker.HealthCheck{}
	Checkable3 = &checker.HealthCheck{}
	Checkable1.GetID(false)
	//Метод изменяет поле структуры у переменной типа интерфейс.
	Checkable2.GetID(true)
	Checkable3.GetID(false)

	go items.Run(ctx, change, 5)

	items.Add(&Checkable1, change)
	items.Add(&Checkable2, change)
	items.Add(&Checkable3, change)

	time.Sleep(10 * time.Second)
	fmt.Println("Ch 2 False. Add Ch 4 false.")
	Checkable2.GetID(false)
	Checkable4 = &checker.HealthCheck{}
	Checkable4.GetID(false)
	items.Add(&Checkable4, change)
	Checkable4.GetID(false)
	fmt.Println("Finish.")
	fmt.Print(items)
	items.Stop(cancel)
*/ 
}

